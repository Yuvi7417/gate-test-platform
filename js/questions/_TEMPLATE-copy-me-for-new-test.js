/* ======================================================================
   TEMPLATE — copy this file to make a NEW test.

   Steps:
   1. Duplicate this file and rename it, e.g. "cs-subjectwise-test-6.js"
   2. Rename the variable below from `playerQuestions` to something like
      `playerQuestions` — keep it named exactly `playerQuestions` since
      that is the name exam.js expects. Only ONE questions-file should be
      loaded on the page at a time (see index.html <script> order notes).
   3. Fill in your real questions below. Delete this comment block once done.
   ====================================================================== */

const playerQuestions = [
  {
    marks: 1, // positive marks for a correct answer
    neg: 0.33, // negative marks for a wrong answer
    text: "Type your question here.",
    image: null, // e.g. "images/test6-q1.png" — or null for no image/diagram
    options: ["Option A", "Option B", "Option C", "Option D"],
    correct: 0, // index of the correct option: 0=A, 1=B, 2=C, 3=D
  },

  // 👉 Copy the block above for each additional question.
];
